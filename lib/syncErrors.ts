// Общий класс ошибки "площадка не настроена для этой компании" — роуты,
// вызывающие lib-функции синка, ловят его отдельно от прочих сбоев API
// (400 "сначала настройте" вместо 502 "не удалось получить данные").
export class MarketplaceNotConfiguredError extends Error {}
